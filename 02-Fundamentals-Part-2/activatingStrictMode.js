'use strict'; // catch the error and showing on console dev

let hasDriversLicense = false;
const passTest = true;

if(passTest) hasDriversLicense = true;
if (hasDriversLicense) console.log('I can drive :D');

// Cannot use below words for variable name
const interface = 'Audio';
const private = 534;